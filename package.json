{
  "name": "lbf-stats",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "tsc && cp -r assets dist/assets && cp .env dist/.env",
    "start": "node dist/index.js",
    "dev": "npm run build && node dist/index.js",
    "dev:watch": "nodemon --watch src --ext ts --exec \"npm run build && node dist/index.js\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^14.21.0",
    "dotenv": "^17.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
